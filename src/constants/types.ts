type MovieName = string;

type Tier = {
    url: string,
    hash: string,
    quality: string,
    type: string,
    size: string,
}

type Movie = {
    poster_path: string
    backdrop_path: string,
    original_title: string,
    overview: string,
    title: string,
    vote_average: string,
    id: string
}